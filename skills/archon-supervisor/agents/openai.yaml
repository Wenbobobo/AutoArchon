interface:
  display_name: "Archon Supervisor"
  short_description: "Supervise long-running Archon proof runs with fidelity checks"
  default_prompt: "Use $archon-supervisor to supervise an isolated Archon run until the scope is solved, a blocker is validated, or a hard stop condition is reached."
