interface:
  display_name: "AutoArchon Supervisor"
  short_description: "Supervise an external AutoArchon run with fidelity and restart checks"
  default_prompt: "Use $archon-supervisor to supervise an external AutoArchon run until the scope is solved, a blocker is validated, or a hard stop condition is reached."
