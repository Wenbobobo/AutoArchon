interface:
  display_name: "AutoArchon Orchestrator"
  short_description: "Coordinate multi-run teacher campaigns and finalize accepted artifacts"
  default_prompt: "Use $archon-orchestrator to create or resume an AutoArchon campaign, deploy teachers, monitor run health, and finalize only validated proofs and blocker notes."
