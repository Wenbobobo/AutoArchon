{
  "campaignRoot": "${CAMPAIGNS_ROOT}/${FATE_DATE_TAG}-fate-m-full",
  "sourceRoot": "${BENCHMARK_ROOT}/FATE-M-upstream",
  "reuseLakeFrom": "${BENCHMARK_ROOT}/FATE-M-upstream",
  "runSpecOutput": "${RUN_SPECS_ROOT}/${FATE_DATE_TAG}-fate-m-full.json",
  "teacherModel": "${MODEL}",
  "teacherReasoningEffort": "${REASONING_EFFORT}",
  "planShards": {
    "matchRegex": "^FATEM/.*\\.lean$",
    "shardSize": 8,
    "runIdPrefix": "teacher-m",
    "runIdMode": "index"
  },
  "watchdog": {
    "model": "${MODEL}",
    "reasoningEffort": "${REASONING_EFFORT}",
    "pollSeconds": 30,
    "stallSeconds": 300,
    "bootstrapLaunchAfterSeconds": 45,
    "maxRestarts": 3,
    "ownerSilenceSeconds": 1200,
    "maxActiveLaunches": 2,
    "launchBatchSize": 1,
    "launchCooldownSeconds": 90,
    "finalizeOnTerminal": true
  },
  "environment": {
    "ARCHON_CODEX_READY_RETRIES": "${ARCHON_CODEX_READY_RETRIES}",
    "ARCHON_CODEX_READY_RETRY_DELAY_SECONDS": "${ARCHON_CODEX_READY_RETRY_DELAY_SECONDS}"
  }
}
